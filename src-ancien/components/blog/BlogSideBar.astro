---
interface Props {
  headings: Array<Object>;
}

const { headings } = Astro.props;
---

<aside>
  <nav class='hidden lg:block sticky top-6'>
    <ul>
      {
        headings.map((heading: any) => (
          <li class='py-1'>
            {[...Array(heading.depth - 1)].map((_, _i) => (
              <p class='ml-4 inline-block' />
            ))}
            <a
              class='text-stone transition-all duration-150 ease-in-out hover:text-dark poppins'
              href={`#${heading.slug}`}
            >
              {heading.text}
            </a>
          </li>
        ))
      }
    </ul>
  </nav>
</aside>
