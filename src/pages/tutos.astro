---
import { getCollection } from "astro:content";
import Layout from "../layouts/Default.astro";
import FilterTags from "../components/components/FilterTags.vue";
import { tagHierarchy } from "../components/tagHierarchy";
import { applyContentScope } from "../utils/content-section";

const allPosts = await getCollection("posts");
const tutosPosts = applyContentScope(allPosts, "tutos");

const initialPosts = tutosPosts
	.sort((a, b) => b.data.pubDate.getTime() - a.data.pubDate.getTime());

const mainTags = Object.keys(tagHierarchy);
---

<Layout
	title="Tutos"
	description="Tutoriels GOCHARBON: guides actionnables, pas à pas, pour exécuter plus vite sur le web."
	pageTitle="Tutos GOCHARBON"
>
	<main class="px-2 md:px-6 py-6 max-w-7xl mx-auto">
		<div id="dynamic-content">
			<FilterTags
				client:visible
				mainTags={mainTags}
				initialPosts={initialPosts}
				scope="tutos"
			/>
		</div>
	</main>
</Layout>

<style>
	.page-background {
		transition: background-color 0.3s ease;
	}
</style>
